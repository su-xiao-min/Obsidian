# 凤凰架构的第四章透明分流系统。

## 客户端缓存

客户端缓存开始。
客户端缓存分为两种策略。

强制换存就是直接要求你缓存一个数据。
你的缓存只能在一定时间内有效，
而协商缓存则是。
我们通过发送请求。
回应，
看是否能够直接利用缓存的数据。
你会发送一个请求，
提供上一次你得到信息的时间，
我会根据这个时间，
决定是直接让你使用缓存，
还是重新发送

强制缓存是基于时效性的。
它强制性能损耗低，
但是它无法跟动态的捕获一些复杂的情况。
而协商缓存则。
需要请求。
不过，
他可以在一些时候。
有更好的表现。
在一致性上表现更加的好。
因为即使强制缓存还没有过期，
也可以刷新。

## 域名解析技术

之后就是我们域名解析技术。

这和我之前去部署我自己网站时候，
遇到的一堆让人苦恼的问题有一些关系。

查询域名的时候，
可以得到一堆奇奇怪怪的东西，
乃至Text，CName。
域名解析它是先递归再迭代。
但是也存在两个问题。
首先是它不安全。
有可能你的本地的DNS是。
危险的，
或者说他会绑架你，
对你的DNS产生污染。

所以说目前有了一种比较厉害的技术，
就是使用HTPS来进行。
域名解析。
当然，
域名解析它也有。
很多的类型。
你解释到的东西的类型可能多种多样，
它也是后来的负载均衡的一些技术实现的基础。
之后就是我们传输链路的优化。

因为TCP的连接是有成本的。
当然，
这就要谈论我们。
HTTP的几版的区别。
总之，
随着HTTP协议的升级，
传输链路优化显得越来越不必要，
目前最好的就是quick。
它是基于CP的。
性能非常的好，
而且可以处理移动端的问题。

因为它和TCP不同的是，
它会为每一次的绘画绑定一个uuid。
这样的话，
即使说你中途更换网络，
但是服务器还认得你。
你们的传输就可以继续进行。
之后就是我们非常有趣的内容分发网络。
内容分发网络还是要和我们的域名解析开始。

我们查询到域名之后会返回一个地址。
但是解析的过程，
这个返回的地址其实可以。
比较多样。
我们使用内容分发网络的时候，
我们可以直接访问一个本地比较近的服务器。
它会缓存服务器的数据。

内容分发也有两种策略，
一种是主动的推送。
这适合于那些高频访问的数据。
就好像我们的缓存也可以选择主动的刷新。
而另外一种则是被动的回溯。
当然，
一般来说，
被动的回溯，
我感觉那你也没有必要缓存了。

CDN的应用。
它不仅可以缓存，
它还有非常多的功能。
首先是它可以堡我们的服务器不直接被攻击到。
它也可以提供协议升级的功能。
即使你的原网站不提供HT。
PS, 你的CDN服务器可以提供。
它也可以提供状态缓存。

换言之，
如果说你的服务器想要直接返回错误码。
那么CDN也可以就直接帮你实现，
它还可以去提供一些升级的功能，
比如说修改我们的资源，
提供访问控制，
或者提供一些原始的。
代码不具备的功能。

最后一部分就是我们的负载均衡。
其实我们的内容分发一定程度上就已经实现了负载均衡。
但是。
过了内容分发这一部分，
我们服务器端也能够实现负载均衡。
首先就是四层负载均衡。
我们可以直接修改我们的Mac地址。

这样就是二层协议。
但是这样限制了你必须在本地网络中才能修改。
之后还有一层，
就是我们的IP地址，
就是3层的负载均衡。
这样，
其实套娃就是IP隧道。
我们得到了IP数据包。
之后我们再把它分装，
打包成一个新的IP数据包，
大家发过去。

之后我们的服务器，
也就是我们。
负载均衡后的服务器，
它会解析这个套娃的IP数据包，
再返回数据。
不过这要求我们的。
新的服务器它要跟我们负载均衡的。
服务器的IP地址一致才可以。
最后一种就是比较彻底的使用net博士相当于。

你的负载均衡器直接修改原地址和目的地址。
之后你的服务器就直接按照原地址来返回。
但是这样我们的负载均衡器就有了比较大的负担。
它其实就是一种内网策略，
而且这样也有一个问题，
就是我们无法在服务器端实现一些基于原IP地址的一些策略。

之后我们再来谈论一下应用层面的复载均衡。
也就是我们的代理技术。
应用层面的复杂均衡就非常的高级。
当然，
越高级，
性能代价就越高。
因为那些比较底层的优化都已经被自定义出来了。
你想要实现自定义，
虽然给了你自有，
但是性能优化就无法保障了。

我们再来讨论一下负载均衡的策略。
你可以使用轮询，
可以使用权重，
可以使用完全随机权重，
完全随机一致性哈希，
或者按照我们的响应速度。
这些不是太难。
当然最后就是到了我们的服务器端缓存。

这也是我学的非常多的一部分。
Radis当时也学了很久，
学习rad的作用。
就是我消息队列学的不是很好，
缓存。
也不是学的很好。
我们需要明白，
就是缓存是有代价的。
我们不能按照多多益善的方式去实现。

我们的缓存在硬件层面我们是有缓存，
但是硬件层面的缓存和软件层面的缓存的副作用肯定是。
差别很大的硬件层面的缓存。
它已经解决了自己的一些问题。
但是在软件层面，
我们就要自己去处理缓存的失效关系一致性的问题。

从运维的层面，
缓存也会带来一些问题。
它会让问题在更久远之后暴露。
暴露在距离问题发生的地方更远的地方。
从安全层面，
缓存也会泄露，
可能会泄露我们的一些保密数据。
但是缓存它有两个作用。

一方面是它可以去缓解我们的CPU压力。
另外一方面，
它也可以去缓存，
解决我们的输入读取的压力。
缓存。
它的主要目的是去缓解我们的压力。
但是我们最好还是直接去提高我们的CPU性能和我们的IO性能。

升级硬件是最好的解决方案。
它远远优于引入缓存之后可能会带来的成本。
因为程序员的工资其实也是比较昂贵的。
当我们引入缓存之后，
我们就要想到为什么我们不能直接使用哈希map或者concurrent。

哈希Map。
来实现我们的缓存。
因为缓存的时候我们需要考虑很多的问题。
我们需要考虑我们的吞吐量、命中率。
扩展功能乃至我们的分布式缓存。
这就好像是我们的数据库。
它作为数据库也需要自己去解决很多的问题。

这也是我们不会自己去实现一个数据库，
而是去选择市场上应有的数据库。
我们不会自己直接在数据库中实现文件管理。
而会去寻找专门的文件管理的方案。
在吞吐量方面，
坎卡尔的哈希。
迈普就已经是最好的方案了。

但是我们肯定需要解决其他的问题。
我们可以使用环形缓冲的方式去提高。
我们的读取性能。
但是写入数据的时候我们不能丢失任何的状态。
所以，
我们必须要使用有界队列来存储我们的变更信息。

之后还有我们的命中率和淘汰策略。
当然淘汰策略比较常见的就有按照最近使用方式来淘汰。
或者照。
优先淘汰最不经常使用的数据。
还有它的优化的策略和一些缓和的策略。
这些我们最好都不要自己手动来实现。

缓存。
它还需要很多扩展的功能。
就比如说我们需要主动的去加载我们的数据，
以及我们需要去选择淘汰策略，
选择我们的失效策略，
乃至我们可以选择一些事件通知并发的功能等等。
之后我们就要谈论我们的分布式缓存。

这样比较好的常见的实现方式就是使用我们redis来实现分布式缓存。
也就reddi相关的内容了。
复制式缓存适合于读多写少的策略。
集中式缓存也就是我们。
常见的缓存策略。
缓存也需要多级策略。
但是，
缓存实现多级。

对于我们代码的侵入性会非常的高。
而且我们需要明白，
任何在缓存中的数据，
我们都不要追求强一致性。
如果想要追求强一致性，
我们就不要使用缓存。
