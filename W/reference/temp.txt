关于标点符号这一点，我感觉，在搜狗输入法中实现有一点困难，因为它需要的敲键数太多，敲键数太多意味着，会打断我输入的节奏。一些标点符号的输入其实只是为了解决我中英文状态下标点符号的变化，但是，我的设置很可惜并没有实现这个功能。我感觉不如我切换输入法来得直接。
不过，某些特殊的输入，我感觉使用Quicker更方便一些，但是你不方便帮我实现。

语法词确实可以统一管理。

代码块模版，这个是为了方便我在Typora当中输入，我但是，后面只能跟随1,2,3,4这样的符号，所以，你其实可以按照py,1这样的方式把常用的再给我添加几个。
包括，xml，html， python ，Javascript，Java，Bash，PowerShell，Rust，R，kotlin，方式都是，形如
py,1=Python
py,2=```python
这样的格式就可以了。

代码块统一成为小写，我感觉你说的很对，你就这样做。

时间格式这一块，就按照你的做法。
个人信息这一块，就按照你的做法。
Markdown格式这一块，除了rw 表示- [ ] 之外，都不需要，因为Typora提供了快捷键。

emoji这一块，就按照你的做法，不过，你后面只能跟着1,2,3,4,5,6，不能其他的方式。

你可以使用emojiok,1这样的写法，这样，我只要记得后面是OK，我就可以输入全部了。
同时，你也最好保留emoji,1这样的写法
你最好把emoji换成em，这样我输入更快捷

纠错词，就按照你的做法。

文件路径，我不太理解你的思路，不过好消息是，后面几个路径我都用得不多。我的主要问题是，一些我常用的路径在WSL和PowerShell当中是不一样的，我也不应该应该怎样输入。你先完成其他的模块，不要动这个领域。

你来帮我整理一下我的相关的信息，这个文件夹当中全部都是我的个人信息，但是它们的结构非常混乱，不方便我后续的添加和维护

语法非常简单

name@@content

请你按照内容创造相关的文件夹。所有的文件都保持是txt格式的，不需要其他的内容。

请你保持name的逻辑性，换言之，命名尽可能整齐，同时，也应该同时包含中英文，方便我模糊检索。

几个问题，

首先，你的Python脚本写得有一点丑陋，你或许可以添加一些类型信息，同时，再来一点注释，我对于Python的理解能力并不好，

同时，
```python
if any(kw in combined_text for kw in ['qq', '微信', 'weChat', 'steam', '知乎', '飞书', '社交']):
```
你既然都使用了这样的内容，你一开头申明分类规则的用意何在呢？

最后，我希望的脚本效果应该是，它直接扫描这个目录，结果也是直接在这个目录中创建一组文件，不需要再来一个子目录结构，没有什么意义。

你再调整一下排序的逻辑吧，

你修改一下main函数的逻辑，直接识别脚本运行目录下的所有md or txt文件，因为我这边是Windows的环境，和你的不太一样。不要把路径写死。

另外，我总结一下你的逻辑，你是逐个文件进行阅读，之后进行逐行解析，之后以@@作为分隔符，接着扫描关键词是否在解析之后的内容当中。

我希望的效果就是，即使反复运行这个脚本，这个目录的结果也不会变，它的效果就是将无序的内容变得有序，它对于有序的结果不会产生影响。


安全性问题，我想一下，你直接把原始的数据打包放进一个bak的文件夹当中，并且去掉文件的后缀名。

 2. 敏感信息安全风险，不解决。

3，自行解决。4，自行解决。

5，不解决。  6. 分类规则可能误判，不解决。



勾指起誓

read,1=read
read,2=Read
read,3=READ
readme,1=README
readme,2=README
ty,1=Typora

undolog,1=Undo Log

你好好想一下再回答我，我的算法从上到下，从左到右，
只有status为1，我才会考虑在计数上面+1
我每探索一个岛屿之后，都会把右边和下边的岛屿标记成2，所以对于，1,1,1，我只会记录一次。
1,1
1,1
我也只会记录成为一次。
同时，已经确定，不是空数组了。
最后，我确实无法处理复杂的岛屿，所以，我是用了相对复杂的逻辑，只有后面的岛屿不是2，也就是说，没有被连接，我才会标记。

think hard 问题根源：你的标记是局部的，只标记直接相邻的格子，而不是像 DFS 那样递归标记整个连通分量。
你再好好想一下，
 对于 ['1','1','1']：
  - 你标记了 grid[0][1] = '2'
  - 但 grid[0][2] 仍然是 '1'，所以当遍历到它时，又会被计数
问题是，我会走到[1,0]这个位置，这时候我就自然会标记[2.0]这样，我总会标记上的。实际上，大部分测试用例我都通过了。
另外，如果是向上或者向左，其实并不影响，你再好好看看，如果遇到了为2的岛屿，我还是会标记它的右侧和下侧的。
你好好想一下。


