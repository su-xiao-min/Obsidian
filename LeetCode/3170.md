```java
   public String clearStars(String S) {
        char[] s = S.toCharArray();
        List<Integer>[] stacks = new ArrayList[26];
        Arrays.setAll(stacks, i -> new ArrayList<>());
        for (int i = 0; i < s.length; i++) {
            if (s[i] != '*') {
                stacks[s[i] - 'a'].add(i);
                continue;
            }
            // 找第一个非空栈，即为最小字母
            for (List<Integer> st : stacks) {
                if (!st.isEmpty()) {
                    st.removeLast();
                    break;
                }
            }
        }

        List<Integer> idx = new ArrayList<>();
        for (List<Integer> st : stacks) {
            idx.addAll(st);
        }
        Collections.sort(idx);

        StringBuilder ans = new StringBuilder(idx.size());
        for (int i : idx) {
            ans.append(s[i]);
        }
        return ans.toString();
    }

```

这样写有什么问题吗？
我好像遇到了泛型擦抹，不能创建泛型数组，
还有，为什么我的list不能调用removeLast()这个方法。
另外一个问题则是，
我能不能这样写idx.stream().map(i -> source[i]).toString();



```java
    public String cS(String s){
        char[] source = s.toCharArray();
        var stacks = new ArrayList[26];
        Arrays.setAll(stacks, i -> new ArrayList<Integer>());
        for(int i = 0; i < source.length; i++){
            if (source[i] != '*'){
                stacks[source[i] - 'a'].add(i);
                continue;
            }
            for(var st : stacks){
                if(!st.isEmpty()){
                    st.remove(st.size() - 1);
                    break;
                }
            }
        }
        List<Integer> idx = new ArrayList<>();
        for(var st : stacks){
            idx.addAll(st);
        }
        Collections.sort(idx);
        return idx.stream().
                map(i -> String.valueOf(source[i])).
                collect(Collectors.joining());
    }
```

虽然，但是这丑陋而且正确，
最后Stream流的collect，需要注意的是，应该把前面转变成字符串，虽然照理说char也可以，但是
很可惜，char不能直接拼接

看一下我的原始写法吧

```java
    public String clearStars(String s) {
        char[] source = s.toCharArray();
//        记录当前最小的字符串，方便之后倒序
        StringBuilder sb = new StringBuilder();

        PriorityQueue<Character> pq = new PriorityQueue<>();
        char min;
        char[] target = new char[source.length];
//        手动维持索引，倒序的时候需要还原，最后也需要这样构建子字符串
        //st 指向要赋值的位置
        //tt 指向被赋值的位置
        int st = 0;
        int tt = 0;
        while(st < source.length){
        //优先处理*
            if(source[st] == '*'){
                min = pq.poll();
//                因为需要保持最小字典序，所以删除最右边的最小值就可以了。
                while(target[tt] != min){
//                    这一步做的事情有一点多，感觉很容易出错
                    //st && tt 都倒退，顺便为source还原和覆盖
//                    但是非常可惜，这里这样做效率不高。
                    source[st--] = target[tt--];
                }
            //最后就是tt--，清理掉这个min，但是，问题是，那么新的min是什么呢？
            //不对，下一轮的时候，当前tt位置的数字会自动清除掉的，所以不需要考虑。
                st++;
            }else{
                pq.add(source[st]);
                target[tt++] = source[st++];
            }
        }

        String s1 = new String(target, 0, tt);
        System.out.println(s1);
        return s1;

    }
```

## 空间和时间

我感觉这一点还是有一些荒诞吧，
答案的写法是什么，
是延迟构建，
我只是得到数组的全部信息，
但是我不构建它，
这样方便我之后调整，

实际上平时写Markdown，
感觉也是如此，
延迟构建最终结果。

