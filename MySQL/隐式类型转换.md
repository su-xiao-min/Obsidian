[MySQL隐式转换造成索引失效 | JavaGuide](https://javaguide.cn/database/mysql/index-invalidation-caused-by-implicit-conversion.html#sql-测试)

总结一下，如果where两边的数据不一致，就会发生类型转换。

但是，表上的类型转换会让表失去索引的能力，就变成了缓慢的全表查询。

所以，一定要避免使用这种隐式转换。

另外，这也是MySQL的温柔，像是PostgreSQL就不存在这种问题，它分分钟给你报错。