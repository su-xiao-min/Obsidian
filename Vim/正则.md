没有太多需要说明的事情
但是需要记住，
在我们的Vim当中，
和标准的正则还是存在差异的。
换言之，
它的断言使用
`\ze` `\zs`
这样的方式。

还是和ChatGPT聊一下吧。

我感觉Vim当中的正则非常特殊
`(⊙_⊙?)`
它使用了`\ze`&`\zs`
表示正向先行断言和正向后行断言

为什么和前面的字符串匹配的被称为先行断言呢？它明明在字符串后面啊。
为什么和后面字符串匹配的被称为后行断言呢？
另外，负向断言应该怎样使用？

还有，断言究竟有怎样的使用场景？
为什么有时候会同时使用多个断言呢？
而且，断言难道不能直接通过位置判定吗？
比如说，放在我们字符串前面的就是后行断言？它的特点仅仅在于，不被捕获而已，
那么，除了这一点，它究竟有什么特殊的？

它也被称为零宽断言，
不占用匹配字符的位置，
这一点有什么意义？
在Vim当中。

请你详细解释。
同时，提出一些题目，帮助我理解断言在郑泽中的价值。

我不太理解
\@=：正向先行
Sweller - 1cee76b4-6d44-4fb9-9745-d3c524b56b30

- second
- third

\@!：负向先行

\@<=：正向后行

\@<!：负向后行
我看不懂上述的操作，
也看不懂
/http\zs:\/\/\@!/
这个正则的含义。它干嘛多出来最后的\@！这部分呢？
断言不是默认都不消耗字符吗？
这几个和前面的\ze & \zs 是什么关系？
另外，我注意到的是，
在Python当中，
后行断言会把 断言放在匹配的内容的左边
先行断言会把断言放在匹配内容的右边，
但是，貌似在我们的Vim当中，统一都是放在结尾的。

操作题

① 匹配不在行首的 # 注释
/# .+\zs.+
匹配被双引号包围，但不是转义的 "
/.+\zs"\zs\\"\@!
因为我不太懂\@！的操作
匹配中文冒号 ：，但只在标题行中（行首非空）
不懂什么叫做标题行。
匹配所有空行，但不包括文件末尾的最后一行
Vim存在文件末尾这个概念吗？
或者
/\v\n\ze.+
设计思考题

如果没有零宽断言，正则还能成立吗？会退化成什么？
我感觉还能够成立，
但是，功能就会有很多限制。
我记得正则貌似是图灵完备的，
换言之，可以使用正则干非常复杂的事情，
不过，
正则主要用在字符匹配上面。

Vim 把断言拆成 \zs / \ze，相比 (?=...)，是更底层还是更原始？
我感觉更原始。

为什么 Markdown 这种“靠正则解释结构”的格式，天然依赖断言？
因为，
你匹配了，
但是，仅仅用来渲染，
而且，你渲染的内容，
显然不包括那些特殊的符号。
比如说我们的`#`

虽然，但是这里ChatGPT确实错的离谱。
或者说，
我的NeoVim对于正则的支持能力不行。
不过，我感觉有了
`\ze` & `\zs`
就足够了。

lllhalo`(⊙_⊙?)`wwwwwwwoo:
很有趣不是。

但是，应该去意识到，
AI是存在幻觉的，
尤其是，
和NeoVim相关的事情。
直接去看教材吧，
不要继续在这里浪费时间了。
