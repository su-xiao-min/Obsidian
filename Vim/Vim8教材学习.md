我打算在这里玩，学习
《Vim 8 文本处理实战》
感觉这本书写的挺好的，
非常新手友好，
就合适我这样的废物。

## word

狭义单词 -> word
广义单词 -> WORD

这也是我我们三个经典操作，
对应的大小写的区别
`w` hello of next word
`W` begin*of of_next_WORD
但是我的NeoVim有着自己的想法。
它也是把`*`视作单词的一部分
begin@end begin——end begin_end`e`end of current word or next word `E`
`b`
`B`

## cc

cw equals ce ,but I don’t think it a problem.

哇，cc 也可以删除整行，
并且进入INSERT MODE ，很有趣。

## u

`u` 和 Ctrl + R 是逆操作，蛮有趣的。

我草，学到新操作了，
可以使用
`:ls`
来查看当前打开的文件。学会了学会了。

![image-20260125094922413](C:\Tymage\image-20260125094922413.png)
很有趣，不是吗？就是你不能在命令行渲染一张图片，
这一点高低太可惜了。
当然，
也不是不可以，
但是想要这样做，
我们就不能使用PowerShell了，
但是这么power 的 shell，
而且还是自带的，
我怎么舍得舍弃呢？
甜蜜蜜的，
怎么舍得舍弃呢？

## 缓冲区

我草，我就说嘛，
缓冲区这么重要的东西，
标签页管理这么基础的功能，
你Vim怎么可能没有实现呢？

使用 `:ls`
就可以查看缓冲区。
使用
`:e`
就可以实现打开任意文件。
使用
`:b`

另外也是作者强烈安利的一系列动作，
我当然要学习了。

]b和[b循环遍历缓冲区

挺好用的，虽然我也不是很理解，但是有一些元字符在这里是不会被渲染的。
`!!!∑(ﾟДﾟノ)ノ``

`split` or `sp`
`vsplit` or `vs`
可以分屏，大大的好，
这样就不需要我去多开进程了。

怎么关闭呢？当然也不是很困难。
使用<kbd>Ctrl</kbd>+ W + Q 就可以了。
`(*^▽^*)`
使用`:q` 就可以关闭窗口同时卸载缓冲区。

之后Ctrl + JKLH 就可以切屏了！
不过，主要是，我这里还想要留下一个单独的完好的命令行窗口，所以我就不切换了。

然后怎么调整宽度呢？
我们当然是使用
`:resize`
or
`:vertical resize`
这些命令

Ctrl + W Q 就可以关闭一个窗口。
其实可以使用
`:q`

![image-20260125095601632](C:\Tymage\image-20260125095601632.png)

很多看不懂的操作。但是超级有趣的，有没有……

可以使用
`:on` 关闭其他窗口，
很好用，有没有
使用
`:bd`
可以删除当前缓冲区，并且关闭窗口。

另外是在PowerShell当中原生的切换窗口和调整窗格大小的方式。

切换就直接使用
<kbd>Alt</kbd></kbd> + ⬆️
<kbd>Alt</kbd></kbd> + ➡️
<kbd>Alt</kbd></kbd> + ⬅️
<kbd>Alt</kbd></kbd> + ⬇️

之后Alt 再加上 Shift，就可以实现调整窗格了。

至于说在NeoVim当中怎么实现类似的效果
或者说Vim
直接使用Ctrl + JKLK

我草，还有标签页这么强大的功能

`:tabnew`

之后就可以看到清爽的新的标签页了。
也就是新的工作区。

NeoVim & Vim的区别究竟是什么
我感觉吧，
好像那些NeoVim中可以实现的功能，
其实对于的Vim也有相应的插件。
貌似是Vim写的来太痛苦了，
很多功能想要实现会比较困难，
原始存在的插件，
都是因为Vim太强大，
才有大佬愿意下功夫。
但是，
其实完全可以把整个Vim重构一下。
所以就出现了NeoVim

至于说LazyVim，
可能就是NeoVim它需要太多自定义的配置，
所以LazyVim就应运而生。
再加上它的懒加载，
可以避免受传统的NeoVim使用太多插件之后
性能快速下降的问题。

然后我就把上面这些问题问我的千问了。
回答确实还可以。

你解释一下LSP、DAP、Tree-sitter等名词，它们在IDE中的价值是什么？在NeoVim当中有怎样的体现。另外，我想要LazyVim写Markdown文本，但是目前它无法生成标题目录，我应该怎样做。

话说我感觉这个自动补全的功能有一点耽误我书写了，我写下一段话之后，如果我不是输入空格，而是直接Enter准备换行，那么它的自动补全可能误以为我想使用它的提示项，我应该怎么解决这个问题。

[千问的回答](https://www.qianwen.com/share/chat/c8169a4d7f104d2fa9fed309983c005e)
怎么点评呢？确实有一点高级，
感觉就是我太笨了。
今天玩够了。
还发现了explorer资源管理器窗口，
太有趣了。
我要开始我的写作了。

使用
`:Explore`
同时可以使用
<kbd>Enter</kbd>
进入某个文件。
使用 `-`
进入上一级目录
使用<kbd>D</kbd>删除
使用R rename

## move

使用f or t

但是我感觉价值不太。
另外我才知道，^B ,
在普通模式下直接输入{}，
表示移动到段落的开头和结尾。
这里的段落和我的理解差不多。

`(` 表示移动到句子的开头
`H`表示移动到窗口的顶部，
但是，不是的，对于我来说，这个操作就是切换标签页，
`L`表示到窗口的底部。
我感觉非常有价值。
因为，
我可以通过Caps Lock + A + JK 实现翻页操作。
我感觉，不如我自定义的快捷键。

## insert

基本的操作都知道，
像是 O & o 就是新一行，
这也是我很喜欢Vim的一点。
另外，还有 `gi` ，
也就是从上一次停止插入的地方继续修改。
`( ͡° ͜ʖ ͡°)``
`ᕕ( ᐛ )ᕗ`
`(▀̿Ĺ̯▀̿ ̿)``
C 可以 可以删除当前光标右边所有的内容
cc 可以试试到行位
但是，
s还比较特别，
它是整个页面的定位，
就类似于surfingkey的f

天天就搁这儿联想……
`￣へ￣`
`φ(゜▽゜*)♪`
`(¬_¬)`

## 文本对象

又，回到了，我想听一首，限定涂鸦。

文本对象也就是
`w`
`)`
`b`
`B`
`p`

常用的好像也就这些
