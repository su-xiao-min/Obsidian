理解文件和缓冲区的区别，
我们处理的文件只是缓冲区当中的映像，
文件存储在磁盘当中，
缓冲区则存在于内存中。
绝大多数操作都针对于缓冲区当中的文件，
也有少部分操作直接写回磁盘。

Ctrl + 6 可以切换文件。
两个文件切换编辑很方便。

Ctrl + W 可以组合出来各种切换的效果

就比如说，<kbd>Ctrl + W + L</kbd> 可以进行切换
虽然直接Ctrl + L就可以了

另外需要说明，

b 相关的操作是Buffer，针对缓冲区，
不针对普通的区域，
如果想要关闭某个多开的窗口
可以使用
Ex命令 :close :only
也可以使用Ctrl + W + Q
