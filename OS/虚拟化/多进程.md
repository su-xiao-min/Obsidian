虽然多进程貌似和并发相关，
但是这里我们的目标还是，
优先使用虚拟化实现多进程

1. 如果只运行一个程序，操作系统怎么保证程序不做任何我们不希望他做的事情
2. 运行一个进程的时候，操作系统如何让他停下来并且切换到另外一个进程，从而实现虚拟化CPU所需的时分共享。



## 保护

对于进程必须要限制，

直接在CPU运行非常快速，
但是有问题，
它必须要有一些扩展的权限，但是不能完全控制系统

因此我们区分用户模式 和 内核模式



为什么系统调用看起来像是过程调用？

因为那些复杂的和操作系统交互的过程，已经被前辈使用汇编语言写好了，我们只需要调用API就可以了。

陷阱表，
在特定的情况下需要做什么事情，
感觉如何可以改写这个表，
就相当于，无敌了，因为这是特权。
里面的程序可以直接操作底层。

## 切换

早期的协作方式非常友好。

怎么处理非法程序，让他们直接出局就可以了，不然，你还要给他们留什么面子吗？