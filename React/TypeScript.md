TypeScript和Javascript差别貌似没有那么大，
甚至可以说，
TypeScript比Javascript更像Java，
或者说，
更像Kotlin，
因为Java还没有可选链来解决空值问题，
空值问题日常爆炸。

变量申明，

函数申明，
可以使用伟大的箭头函数，
甚至可以使用高阶函数，
只要你看得懂，
它就可以作为返回值。

比较特别的是类型别名。

还可以使用类型推断。
类型推断、类型守卫，都是我们Java儿不了解的东西，
也不算吧，
Java有这些东西，只不过实现可能没有这么优雅。

这个错误处理都和Java如出一辙。

要从多个子组件收集数据，或让两个子组件相互通信，请改为在其父组件中声明共享 state。父组件可以通过 props 将该 state 传回给子组件。这使子组件彼此同步并与其父组件保持同步。

信息传递，
我都快忘记了当时学习Vue的时候看父子通信传递时候的困境。
